<% if task.errors.any? %>
  <div id="error_explanation" class="alert alert-warning">
    <ul class="mb-0">
      <% task.errors.full_tasks.each do |task| %>
        <li><%= task %></li>
      <% end %>
    </ul>
  </div>
<% end %>